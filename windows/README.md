# Windows host

This is the standard Flutter Win32 x64 runner plus CMake integration for `abzar_core.dll`. Runner resources own the executable icon and version metadata. Generated plugin and ephemeral Flutter files do not belong in source control.
