# Dart tests

Fast model, service-contract, localization, and widget tests belong here. Tests must not require signing secrets or upload fixtures. Native format structure belongs in `native/tests`; real-device flows belong in `integration_test`.
