# Native third-party dependencies

CMake resolves permissively licensed dependencies here or with pinned `FetchContent` declarations. The compatibility header is declarations only and links to unmodified zlib. Full license texts belong in `LICENSES/`; versions and roles are tracked in `docs/THIRD_PARTY.md`. Never copy a dependency without its license and provenance.
