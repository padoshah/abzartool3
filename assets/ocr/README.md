# OCR data

Place official Tesseract `eng.traineddata` and `fas.traineddata` here only when `ABZAR_ENABLE_OCR` is enabled. Large trained-data files are release dependencies rather than source placeholders; checksums and Apache-2.0 notices must be recorded in `docs/THIRD_PARTY.md`.
