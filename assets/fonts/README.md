# Fonts

AbzarFile defaults to platform fonts so Persian shaping uses the operating system's maintained fallback. Permissively licensed UI/signature font binaries may be added here only with their license file and a `pubspec.yaml` declaration.
