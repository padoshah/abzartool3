# Instructions for coding agents

Follow `AGENTS.md` as the authoritative repository policy. In particular: perform real transformations; keep files offline; never handle secrets; preserve the importer → `DocModel` → exporter contract; maintain the C ABI; localize all UI text; add matrix entries, fixtures, validators, tests, and docs together; run all checks before committing; and use `tool/set_version.dart` for releases.
