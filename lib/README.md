# Flutter application

`app/` owns composition and navigation, `core/` owns infrastructure and domain primitives, `features/` owns user workflows, and `shared/` owns reusable presentation. Native format logic does not belong in Dart widgets.
