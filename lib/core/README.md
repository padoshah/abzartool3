# Core Dart layer

Cross-feature models, services, error mapping, utilities, and the FFI/isolate boundary belong here. Core must not import a feature screen. User-visible strings remain in ARB resources.
